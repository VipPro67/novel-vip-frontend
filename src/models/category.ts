export interface Category {
  id: string
  name: string
  description?: string
}

export interface Genre {
  id: string
  name: string
  description?: string
}

export interface Tag {
  id: string
  name: string
  description?: string
}
